# Base image for Medusa v2
FROM node:20-alpine

RUN apk add --no-cache libc6-compat git

# Enable Corepack for Yarn 4
RUN corepack enable

WORKDIR /app

# This keeps the container running so you can enter the terminal
CMD ["tail", "-f", "/dev/null"]